{
  "name": "arai-test-cloudflare-workers",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.18.0",
    "typescript": "^4.8.4",
    "wrangler": "2.1.12"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev --local --persist",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "itty-router": "^2.6.6"
  }
}
